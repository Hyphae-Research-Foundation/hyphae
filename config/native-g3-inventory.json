{
  "schema": "hyphae-native-g3-inventory-v1",
  "gate": "G3",
  "requirements": [
    {"id": "strings-and-counters", "status": "implemented-unhosted", "present": ["binary scalar SET/GET/DELETE", "NX/XX", "signed canonical INCRBY", "seeded counter/reopen model"], "gaps": ["hosted suite-bound exact-SHA receipt", "expected-version SET remains a non-claim"]},
    {"id": "hashes", "status": "implemented-unhosted", "present": ["typed lifecycle", "field CRUD/batches/counter/scans", "whole and field TTL", "randomized hash model with negative control"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "lists", "status": "implemented-unhosted", "present": ["chunked deque", "push/pop/range", "whole lifecycle and TTL", "seeded lifecycle/TTL/range model with periodic reopen", "perturbed-oracle negative control"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "sets", "status": "implemented-unhosted", "present": ["member CRUD/batches/scans", "algebra", "whole lifecycle and TTL", "seeded lifecycle/TTL/content model with periodic reopen", "perturbed-oracle negative control"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "sorted-sets", "status": "implemented-unhosted", "present": ["dual-index CRUD", "rank and score ranges", "whole-family lifecycle and TTL", "seeded rank/range model", "rollback negative control", "crash boundaries"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "streams", "status": "implemented-unhosted", "present": ["append-ordered durable format", "stable serialized entry IDs", "read/range/lifecycle/TTL", "seeded lifecycle/TTL/range model", "perturbed-oracle negative control", "conflict and crash matrices"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "ttl-and-controlled-expiry", "status": "implemented-unhosted", "present": ["deterministic logical-time expiry for all six families", "ordered expiry index", "single-CSN all-family cleanup", "bounded cleanup and reconstruction", "all-family visibility and reopen matrix", "TTL index retirement on explicit lifecycle deletion"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "atomic-batches-and-conflicts", "status": "implemented-unhosted", "present": ["multi-field/member atomic batches", "MVCC first-committer-wins", "all-family loser-publishes-nothing matrix", "group commit"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "model-based-randomized-equivalence", "status": "implemented-unhosted", "present": ["models for scalar/counter, hash, list, set, sorted set, and stream", "fixed seeds and periodic reopen", "lifecycle/TTL/error/range alphabets", "negative controls for every family"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "restart-equivalence", "status": "implemented-unhosted", "present": ["periodic model reopen", "per-family crash matrices", "one complete all-family reopen matrix"], "gaps": ["hosted suite-bound exact-SHA receipt"]},
    {"id": "memory-amplification", "status": "implemented-unhosted", "present": ["mixed six-family workload", "logical live and mutation-byte denominators", "separate retained-page and append-page/WAL metrics", "bounded deterministic regression thresholds", "hosted peak-RSS ceiling in the receipt contract", "blob lifecycle compaction/vacuum/collection suites"], "gaps": ["hosted suite-bound exact-SHA receipt"], "out_of_scope_non_claims": ["production-scale thresholds remain G7 evidence"]}
  ]
}
