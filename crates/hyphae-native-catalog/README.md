# hyphae-native-catalog

Internal, unpublished immutable catalog model shared by Hyphae's native
relational, structure, and search engines.

The normative target contract is
[`docs/native/catalog-v1.md`](../../docs/native/catalog-v1.md). Persistence and
WAL publication are not implemented yet, so this crate is partial G1 evidence.
