# Third-party notices

This package's development and optional peer dependency graph is recorded with
exact integrity and license metadata in `package-lock.json`. It is not bundled
into the package. In particular, sharp's optional libvips binaries are
LGPL-3.0-or-later and are development tooling only. See the repository
`THIRD_PARTY_NOTICES.md` for the corresponding-source and relinking obligations.
