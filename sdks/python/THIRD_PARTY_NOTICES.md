# Third-party notices

The Python SDK has no runtime dependencies. Its exact build dependency is
`setuptools==84.0.0`, licensed MIT; setuptools is used by the build environment
and is not bundled into the wheel or source distribution.
